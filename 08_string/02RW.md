## [Reverse words in a string & https://leetcode.com/problems/reverse-words-in-a-string/description/]

**Rating:** (Medium)
**Topic:** (String, Stack, Array)
**Pattern:** (Concatenation of words, String, Array)

### 🧠 The "Aha!" Moment (The Logic)

_Given an input string, containing upper-case and lower-case letters, digits, and spaces( ' ' ). A word is defined as a sequence of non-space characters. The words in s are separated by at least one space. Return a string with the words in reverse order, concatenated by a single space._

_Ex: "We defined two variables, result and a word, result to give final output after joining it, and word to keep a sensible word and concanetate it in the string. We used a for loop going from s.length - 1 to 0 and if we get empty spaces in a string at anypoint then we will just stop concanetating that into the word or else we will and then we will push that word into result once we find an empty space and reset the word to 0 and then we will push a final time after for loop is over and just return the string"_

### ⚠️ The Trap (Where did I get stuck?)

- "I was reversing the charachters instead of words. Needed to makea a new word variable"
- "I forgot to write the reverse logic innto the for loop."

### ⚡ Complexity

- **Time:** O(N)
- **Space:** O(N)
